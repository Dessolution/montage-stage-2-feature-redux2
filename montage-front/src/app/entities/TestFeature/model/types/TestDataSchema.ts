export interface TestDataSchema {
    userId?: number ,
    id?: number ,
    title?: string,
    body?: string,
    isLoading?: boolean,
    error?: string 
}

export interface TestData {
    userId?: number,
    id?: number,
    title?: string,
    body?: string,
}
